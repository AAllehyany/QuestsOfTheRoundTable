package group52.comp3004.GUI;

import javafx.fxml.FXMLLoader;
import javafx.scene.layout.GridPane;
import javafx.scene.layout.Pane;

public class PlayerArea {
	private GridPane root;
	
	//Constructor
	public PlayerArea(GridPane aRoot) throws Exception{
		/*FXMLLoader loader = new FXMLLoader(getClass().getResource("/fxml/player_area.fxml"));
		root = loader.load();*/
		root = aRoot;
		
		//Add the subfields
		//each area type adds itself to root pane in its constructor
		//should probably make area a abstract or interface that each area type inherits from(can do that later)
		FieldArea field = new FieldArea(root);
		HandArea hand = new HandArea(root);
		PortraitArea portrait = new PortraitArea(root);
		//root.add(field, 2, 4, 2, 1); //first set is col/row location 2nd is colspan/rowspan
		//root.add(hand, 2, 5, 2, 1);
		//root.add(portrait, 1, 5);
		
		System.out.println("Player area created");
	}
}
